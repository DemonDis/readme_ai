# Максимальная длина сырого diff'а в символах для diff-only режима.
# Если diff превышает лимит — используется repomix.
README_AI_DIFF_MAX_SIZE=5000

# Обрезать diff до скольки символов перед отправкой AI.
# Работает в обоих режимах (diff-only и repomix).
README_AI_DIFF_TRUNCATE=8000
